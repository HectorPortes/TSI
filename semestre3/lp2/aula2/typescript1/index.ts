let idade: number = 25

let nome: string
nome = 'Tucunarelson'

let atibo: boolean = true

let notas: number[] = [1, 2, 3, 4]

let pessoa: [string, number] = ['João', 20]

let valor: any = 'Pode ser qualquer coisa'

let doisTipos: number | string = 'ABCD'

function soma(num1: number, num2: number): number {
  return num1 + num2
}