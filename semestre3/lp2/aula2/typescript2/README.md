# Iniciando projeto node
npm init -y

# Instalando o typescript
npm i typescript --save-dev

# Gerar JS / traduzir de ts para js
npx tsc index.ts
 
# Gerar JS / traduzir de ts para js com tsconfig.json
npx tsc